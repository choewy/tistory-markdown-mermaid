export enum TtsProvider {
  GOOGLE = 'google',
  TYPECAST = 'typecast',
}

export enum TtsGoogleLanguage {
  KO_KR = 'ko-KR',
}

export enum TtsTypecastLanguage {
  AUTO = 'auto',
}

export enum TtsStatus {
  ACTIVE = 'active',
  DISABLED = 'disabled',
  DELETED = 'deleted',
}
