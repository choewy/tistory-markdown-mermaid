export enum StickerType {
  GENERIC = 'generic',
  SUPER = 'super',
  SOUND = 'sound',
}

export enum StickerStatus {
  ACTIVE = 'active',
  DISABLED = 'disabled',
  DELETED = 'deleted',
}

export enum StickerCategoryScope {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export enum StickerCategoryStatus {
  ACTIVE = 'active',
  DISABLED = 'disabled',
  DELETED = 'deleted',
}
